import 'package:flutter/material.dart';

class ChapterMenuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Accueil')),
      body: Center(child: Text("Bienvenue dans Chapter Menu 👋")),
    );
  }
}
